// The app's own accent palette rather than canvas-confetti's rainbow default,
// so every celebratory burst reads as this product's, not a generic effect.
// Shared by every full-screen "moment" overlay that fires confetti.
export const CONFETTI_COLORS = ['#4169e1', '#04adc0', '#8b7cf6', '#f59e0b', '#6ee7a8']
